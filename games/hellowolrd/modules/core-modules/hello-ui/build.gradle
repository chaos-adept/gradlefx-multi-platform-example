buildscript {
	repositories {
		mavenCentral()
	}
	dependencies {
		classpath group: 'org.gradlefx', name: 'gradlefx', version: '0.6.4'
	}
}

apply plugin: 'gradlefx'

type = 'swf'

frameworkLinkage = 'none'

additionalCompilerOptions = ['-tools-locale=en'];

mainClass = 'HelloWorldDisplay.as'
